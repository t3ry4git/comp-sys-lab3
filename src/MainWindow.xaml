<Window x:Class="comp_sys_lab3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Neural Network Visualizer" Height="600" Width="800" WindowStyle="ToolWindow" ResizeMode="CanMinimize">
    <Grid>
        <Canvas x:Name="NetworkCanvas" Background="#FF3C3C3C" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="200,10,10,10"/>
        <StackPanel Orientation="Vertical" Width="180" HorizontalAlignment="Left" Margin="10,10,10,10">
            <Button Content="Add Layers" Click="AddLayers" Margin="5"/>
            <Button Content="Remove Layers" Click="RemoveLayers" Margin="5"/>
            <Button Content="Send Inputs" Click="SendInputs" Margin="5"/>
            <RichTextBox x:Name="MyTextBox" Height="445" VerticalScrollBarVisibility="Visible">
                <FlowDocument>
                    <Paragraph>
                        <Run Text=""/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
        </StackPanel>
    </Grid>
</Window>
